apiVersion: "nais.io/v1alpha1"
kind: "Application"
metadata:
  name: kontakt-oss
  namespace: default
  labels:
    team: teamtag
spec:
  image: navikt/kontakt-oss:{{version}}
  team: teamtag
  port: 3000
  vault:
    enabled: true
  ingresses:
    - https://arbeidsgiver-q.nav.no/kontakt-oss/
  liveness:
    path: /kontakt-oss/internal/isAlive
    initialDelay: 20
  readiness:
    path: /kontakt-oss/internal/isReady
    initialDelay: 20
  env:
    - name: API_GATEWAY
      value: https://api-gw-q0.oera.no
    - name: NODE_EXTRA_CA_CERTS
      value: /etc/ssl/ca-bundle.pem
